@model IEnumerable<ProyectoInmobiliariaMVCPrimera_Entrega.Models.Usuario>

@{
    ViewData["Title"] = "Index";
}

<h1>Usuario</h1>

<p>
    <a asp-action="Create">Crear usuario</a>
</p>
<table class="table">
    <thead>
        <tr>
            @*<th>
                @Html.DisplayNameFor(model => model.UsuarioId)
            </th>*@
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Apellido)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.Clave)
            </th>*@
            <th>
                @Html.DisplayNameFor(model => model.Rol)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
    //if(item.RolNombre =="Administrador"){

    //}
        <tr>
        @*<td>
            @Html.DisplayFor(modelItem => item.UsuarioId)
        </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Apellido)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.Clave)
            </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.RolNombre)
            </td>
            <td>
                @*@Html.ActionLink("Editar", "Edit", new { id=item.UsuarioId}) |*@

                <a asp-action="Edit" asp-route-id="@item.UsuarioId" title="Editar">
                <span class="lnr lnr-pencil">Editar</span></a>&nbsp;

                @Html.ActionLink("Eliminar", "Delete", new { id = item.UsuarioId })
            </td>
        </tr>
}
    </tbody>
</table>
